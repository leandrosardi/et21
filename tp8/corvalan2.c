#include <stdio.h>
int main(){
char Nombre[] ="Matias";
printf("Nombre: %s\n";
    char *puntero = nombre;
    int cantidad = 0;
    int vocales = 0;
    while (*puntero != '\0') {
printf("%c\n", *puntero);
printf("ASCII: %d\n", *puntero);
if(*puntero == 'a'
         *puntero =='o'
             *puntero =='A'
             *puntero =='I'
                     *puntero== 'U'){
                     *puntero== 'e'
                     *puntero== 'u'
                     *puntero== 'E'
                     *puntero== 'O'
                     *puntero== 'i'
        
             vocales++;

    }

    cantidad++;
    puntero++;

    }

printf("Cantidad de letras: %d\n", cantidad);
printf("Cantidad de vocales: %d\n", vocales);
 
   return 0;
   
}